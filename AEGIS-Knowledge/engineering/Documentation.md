# Documentation (code-level)

- Docstrings explain *why* and *contract* (inputs, outputs, side effects, failure modes) — not a restatement of the function body.
- Keep examples in docs runnable/testable where possible so they can't silently go stale.
- Update docs in the same diff as the behavior change, not "later."
