# Layered Architecture

Classic Presentation → Business Logic → Data Access layering. Simple and well-understood, but layers can become a false sense of separation if business logic leaks into the presentation or data layer — enforce the boundary with code review, not just folder names.
