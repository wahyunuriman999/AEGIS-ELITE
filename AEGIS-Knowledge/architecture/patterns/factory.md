# Factory Pattern

Encapsulate object construction logic behind a function/class, especially when creation involves choosing among implementations or non-trivial setup. Use when construction logic would otherwise be duplicated at every call site; skip it for a plain constructor with no branching.
