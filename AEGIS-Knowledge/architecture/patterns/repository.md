# Repository Pattern

Abstract data access behind an interface (`OrderRepository.findById`) so business logic doesn't depend on a specific storage technology or query language. Pairs naturally with Hexagonal/Clean Architecture as a "port". See also `database/patterns/repository.md` for the storage-side implementation concerns.
