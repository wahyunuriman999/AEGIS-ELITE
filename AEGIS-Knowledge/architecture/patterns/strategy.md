# Strategy Pattern

Encapsulate an interchangeable algorithm/policy behind a common interface (e.g. different pricing strategies, different retry policies), selected at runtime. Use when you have genuine, growing variation in behavior — not for a single `if/else` that will never gain a third branch.
