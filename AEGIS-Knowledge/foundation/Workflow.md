# Workflow

The end-to-end loop (mirrors the kernel's Operating Loop):
Diagnose → Plan → Execute → Verify & Gate → Reflect & Report.
Foundation governs the judgment inside each step; it doesn't replace the step.
