# Repository Pattern (data-access side)

Isolate persistence logic (queries, ORM calls) behind an interface the domain layer depends on, so storage technology can change without touching business logic. See also `architecture/patterns/repository.md` for the interface-side view.
